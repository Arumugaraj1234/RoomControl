import 'package:flutter/material.dart';

class LightModel {
  String iconName;
  String title;
  Color bgColor;
  Color textColor;

  LightModel({this.iconName, this.title, this.bgColor, this.textColor});
}
